# 2025-11-23 环境修复记录

- 在 `cbt-diagnostic-app` 目录下执行 `npm install --legacy-peer-deps`，解决 eslint / eslint-plugin-react-hooks 的依赖冲突并完成依赖安装。
- 启动开发服务器：在 `cbt-diagnostic-app` 目录执行 `npm run dev`，Vite 成功启动（本地端口起始为 5173，被占用后自动使用 5177）。
- 安装缺失依赖并保证运行时正常：
  - 新增运行时依赖：`framer-motion`、`lucide-react`、`clsx`、`tailwind-merge`（使用 `--legacy-peer-deps` 避免再次触发 eslint 相关 peer dependency 冲突）。

> 注意：`npm audit` 仍提示 2 个 moderate 等级漏洞，当前未处理（为避免引入新的 breaking changes）。如需安全加固，可后续单独评估并执行 `npm audit fix` 或 `npm audit fix --force`。